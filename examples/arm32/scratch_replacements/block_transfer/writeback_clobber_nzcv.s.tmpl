@ scratch-template: v1
@ family: block_transfer
@ op: writeback
@ algorithm: adds-or-subs-byte-count
@ flags: clobber_nzcv
@ kind: fragment
@ temps:
@ inputs: %base,%byte_count
@ outputs: %base,nzcv
@ placeholders: %wb_op_s,%base,%byte_count
@ notes: %wb_op_s is adds or subs, chosen from U bit; omitted when original W bit is clear.

%wb_op_s %base, %base, #%byte_count
